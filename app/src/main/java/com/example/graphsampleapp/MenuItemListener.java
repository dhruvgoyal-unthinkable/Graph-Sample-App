package com.example.graphsampleapp;

import android.view.MenuItem;

public class MenuItemListener {
    public static boolean itemListener(MenuItem item) {

        if (item.getItemId() == R.id.steps) {
           
        }
        //add items here
        return false;
    }
}
